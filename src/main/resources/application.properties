spring.application.name=certifica-utf-back-end
spring.data.mongodb.uri=${MONGO_URL}
spring.security.oauth2.client.registration.google.clientId=${GOOGLE_CLIENTE_ID}
security.jwt.token.secret-key=${TOKEN_SECURITY_JWT}

server.port=${PORT:0}

eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}

utfpr.baseurl=${UTFPR_BASEURL}

